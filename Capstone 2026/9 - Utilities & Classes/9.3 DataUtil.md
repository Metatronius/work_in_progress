Utility functions:

- `validateDate(dateString: String)` – Validates date format

- `getPriority(name: String)` – Converts name to enum

- `getPriorityName(ordinal: Int)` – Converts ordinal to name


Next: [[9.4 Extensions]]